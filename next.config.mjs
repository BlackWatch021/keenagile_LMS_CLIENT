/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "strapimediaupload.s3.ap-south-1.amazonaws.com"],
  },
};

export default nextConfig;
// strapimediaupload.s3.ap-south-1.amazonaws.com
